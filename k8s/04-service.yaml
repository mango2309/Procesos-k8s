apiVersion: v1
kind: Service
metadata:
  name: taller-app-svc
  labels:
    app: taller-app
spec:
  type: NodePort
  selector:
    app: taller-app
    managed-by: deployment
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
